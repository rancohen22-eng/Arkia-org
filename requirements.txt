fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0

# expense module — PDF generation (pure-Python, no system libs, no browser)
fpdf2==2.8.7
python-bidi>=0.4.2
Pillow>=10.0.0

# optional — passkey / WebAuthn login. Install only if you enable passkeys
# (set WEBAUTHN_RP_ID / WEBAUTHN_ORIGIN). The app runs fine without it.
# webauthn>=2.0.0
